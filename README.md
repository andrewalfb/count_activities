# Count Activities

React + TypeScript app for tracking hobbies, time, and statistics.

## Run

Frontend:

```bash
npm start
```

Backend:

```bash
cd backend
node index.js
```

## Notes

- Translation data lives in `src/translation/`.
- Root `package-lock.json` is intentionally not committed in this repo.
